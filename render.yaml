services:
  - type: web
    name: veritas-blue-web
    env: node
    buildCommand: chmod +x deploy.sh && ./deploy.sh
    startCommand: cd .. && npm start
    envVars:
      - key: NODE_ENV
        value: production 